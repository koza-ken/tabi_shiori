<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body data-controller="hamburger">
    <%= render "shared/header" %>
    <div class="relative min-h-screen">
      <%= yield %>
      <!-- 背景オーバーレイ -->
      <div class="absolute inset-0 bg-black/50 z-40 hidden" data-hamburger-target="overlay" data-action="click->hamburger#toggle"></div>
      <!-- メニューパネル -->
      <nav class="absolute top-0 right-0 h-screen w-64 bg-white shadow-lg z-50 hidden" data-hamburger-target="menu"></nav>
    </div>
  </body>
</html>
